---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: wireguards.vpn.ahova.com
spec:
  group: vpn.ahova.com
  names:
    kind: Wireguard
    listKind: WireguardList
    plural: wireguards
    singular: wireguard
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WireguardPeer is the Schema for the wireguardpeers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              address:
                default: 192.168.254.1/24
                description: Address space to use
                pattern: ^((10(\.(([0-9]?[0-9])|(1[0-9]?[0-9])|(2[0-4]?[0-9])|(25[0-5]))){3})|(172\.((1[6-9])|(2[0-9])(3[0-1]))(\.(([0-9]?[0-9])|(1[0-9]?[0-9])|(2[0-4]?[0-9])|(25[0-5]))){2})|(192\.168(\.(([0-9]?[0-9])|(1[0-9]?[0-9])|(2[0-4]?[0-9])|(25[0-5]))){2})|(127('\.(([0-9]?[0-9])|(1[0-9]?[0-9])|(2[0-4]?[0-9])|(25[0-5]))){3}))/([8-9]|(1[0-9])|(2[0-9])|(3[0-2]))$
                type: string
              affinity:
                $ref: '#/definitions/k8s.io~1api~1core~1v1~0Affinity'
                description: Affinity configuration
              allowedIPs:
                default: 0.0.0.0/0, ::/0
                description: IP addresses allowed to be routed
                type: string
              dns:
                description: DNS configuration for peer
                properties:
                  address:
                    description: Address is an IPV4 address of the DNS server
                    type: string
                  deployServer:
                    description: Indicates whether to use internal kubernetes dns
                    type: boolean
                type: object
              dropConnectionsTo:
                default:
                - 192.168.0.0/16
                - 172.16.0.0/12
                - 10.0.0.0/8
                - 169.254.169.254/32
                description: Do not allow connections from peer to DropConnectionsTo
                  IP addresses
                items:
                  type: string
                type: array
              endpointAddress:
                description: Public address to the wireguard network
                type: string
              replicas:
                default: 1
                description: Replicas defines the number of Wireguard instances
                format: int32
                maximum: 3
                minimum: 1
                type: integer
              serviceAnnotations:
                additionalProperties:
                  type: string
                description: Annotations for the service resource
                type: object
              serviceType:
                $ref: '#/definitions/k8s.io~1api~1core~1v1~0ServiceType'
                default: ClusterIP
                description: Type of the service to be created
              sidecars:
                description: Sidecar containers to run
                items:
                  $ref: '#/definitions/k8s.io~1api~1core~1v1~0Container'
                type: array
            type: object
          status:
            properties:
              endpoint:
                type: string
              publicKey:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
